// ---- Model Dump
module 0x8675309::M {
    private fun any<T>(): #0 {
        Abort(0)
    }
    private fun foo(x: &mut u64) {
        Freeze<u64>(x);
        Freeze<vector<bool>>(Borrow(Mutable)(M::any<vector<bool>>()));
        Abort(Deref(x));
        Tuple();
        Tuple()
    }
    spec fun $any<T>(): #0 {
        Abort(0)
    }
    spec fun $foo(x: &mut u64);
} // end 0x8675309::M
